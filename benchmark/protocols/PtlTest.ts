import { uint } from '../../src/index';
export interface ReqTest {
    a?: uint;
    b?: string;
    c?: boolean;
    d?: Uint8Array;
}

export interface ResTest {
    a?: uint;
    b?: string;
    c?: boolean;
    d?: Uint8Array;
}