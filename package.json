{
  "private": true,
  "devDependencies": {
    "lerna": "^5.1.8"
  },
  "scripts": {
    "init": "npm i && lerna bootstrap",
    "test": "lerna run test",
    "build": "lerna run build",
    "publish:dev": "",
    "publish:latest": ""
  }
}
