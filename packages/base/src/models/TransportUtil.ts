import { TransportData } from "../base/BaseConnection";

export class TransportUtil {

    static transportData2Buf(data: TransportData): Uint8Array {
        throw new Error('TODO')
    }

    static buf2TransportData(buf: Uint8Array): TransportData {
        throw new Error('TODO')
    }

}